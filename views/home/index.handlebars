<section id="top_decks">
    <ol>
        {{#each decks}}
        <li>
            <a href="/deck/{{this._id}}">{{this.name}}</a>
            <p>{{this.description}}</p>
        </li>
        {{/each}}
    </ol>
</section>